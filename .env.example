# AISStream.io - darmowy klucz API (rejestracja przez GitHub)
AISSTREAM_API_KEY=your_api_key_here

# Tryb: "demo" = symulowane dane | "live" = AISStream | "replay" = odtwarzanie z pliku
MODE=demo

# Sciezka do pliku CSV z danymi historycznymi (tryb replay)
# REPLAY_FILE=data/ais_gdansk_2026-07-15.csv
